[package]
name = "compukter-ffi"
version.workspace = true
edition = "2021"
license = "Apache-2.0"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
compukter-vm = { path = ".." }

[dev-dependencies]
sha2 = "0.10"
