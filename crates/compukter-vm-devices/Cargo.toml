[package]
name = "compukter-vm-devices"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
repository = "https://github.com/CertifiedBadIdeas/Compukter-VM"
description = "Deterministic bounded device models for Compukter VM"

[dependencies]
compukter-vm = { path = "../..", version = "0.1.0" }
