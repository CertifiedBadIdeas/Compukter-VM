[package]
name = "persistence-crash-fixture"
version.workspace = true
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]
compukter-vm = { path = "../..", features = ["persistence-crash-testing"] }
sha2 = { version = "0.10", default-features = false }
